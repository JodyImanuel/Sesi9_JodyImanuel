{
  "name": "webuiautomation",
  "version": "1.0.0",
  "description": "WEB Automation Testing",
  "type": "module",
  "scripts": {
    "test": "mocha test --recursive --timeout 60000 --reporter mochawesome"
  },
  "dependencies": {
    "selenium-webdriver": "^4.35.0"
  },
  "devDependencies": {
    "mocha": "^11.7.1",
    "mocha-simple-html-reporter": "^2.0.0",
    "mochawesome": "^7.1.3"
  }
}
